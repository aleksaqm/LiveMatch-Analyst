package backward

import com.ftn.sbnz.model.models.User;

rule "Aleksa is King"
    when
        $u : User(name == "Aleksa")
    then
        $u.setStatus("King");
        System.out.println($u.getName() + " is King!");
end